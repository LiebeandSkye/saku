<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- AnkiDroid Inter-Process Database Permission -->
    <uses-permission android:name="com.ichi2.anki.permission.READ_WRITE_DATABASE" />
    
    <!-- Gemini AI API Internet Access -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Notification & Lock Screen Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Package Visibility for AnkiDroid & Parallel Builds (Required on Android 11+) -->
    <queries>
        <package android:name="com.ichi2.anki" />
        <package android:name="com.ichi2.anki.a" />
        <package android:name="com.ichi2.anki.b" />
        <package android:name="com.ichi2.anki.debug" />
        <provider android:authorities="com.ichi2.anki.flashcards" />
        <provider android:authorities="com.ichi2.anki.a.flashcards" />
        <provider android:authorities="com.ichi2.anki.b.flashcards" />
        <provider android:authorities="com.ichi2.anki.debug.flashcards" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Boot & App Update Receiver -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Lock Screen / AOD Persistent Foreground Service -->
        <service
            android:name=".notification.LockScreenCardService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Flashcard widget service for lockscreen &amp; AOD display" />
        </service>

        <!-- Lock Screen & Notification Action Receiver -->
        <receiver
            android:name=".notification.NotificationActionReceiver"
            android:exported="false" />

        <!-- Home Screen AppWidget Receiver -->
        <receiver
            android:name=".widget.SakuWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/saku_widget_info" />
        </receiver>

    </application>

</manifest>
